trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: AzurePowerShell@5
  inputs:
    azureSubscription: $(AzureSubscription)
    ScriptType: 'FilePath'
    ScriptPath: './Deploy-AzTemplate.ps1'
    ScriptArguments: -Location 'centralus' -ResourceGroupName $(ResourceGroupName) -TemplateFile application-workloads/active-directory/active-directory-new-domain/azuredeploy.json
    azurePowerShellVersion: 'LatestVersion'