{
  "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "nodeCount": {
      "type": "int",
      "metadata": {
        "description": "Number of nodes in the cluster"
      }
    },
    "vmSize": {
      "type": "string",
      "metadata": {
        "description": "Size of nodes in the cluster"
      }
    },
    "adminUsername": {
      "type": "string",
      "metadata": {
        "description": "Admin username"
      }
    },
    "adminPassword": {
      "type": "securestring",
      "metadata": {
        "description": "Admin username"
      }
    }
  },
  "variables": {
    "templateUrl": "https://raw.githubusercontent.com/benofben/azure-quickstart-templates/master/couchbase/nested/",
    "extensionUrl": "https://raw.githubusercontent.com/benofben/azure-quickstart-templates/master/couchbase/scripts/",
    "uniqueString": "[uniquestring(resourceGroup().id, deployment().name)]"
  },
  "resources": [
    {
      "name": "nodes",
      "type": "Microsoft.Resources/deployments",
      "apiVersion": "2016-02-01",
      "properties": {
        "mode": "Incremental",
        "templateLink": {
          "uri": "[concat(variables('templateUrl'), 'nodes.json')]",
          "contentVersion": "1.0.0.0"
        },
        "parameters": {
          "nodeCount": {
            "value": "[parameters('nodeCount')]"
          },
          "vmSize": {
            "value": "[parameters('vmSize')]"
          },
          "adminUsername": {
            "value": "[parameters('adminUsername')]"
          },
          "adminPassword": {
            "value": "[parameters('adminPassword')]"
          },
          "templateUrl": {
            "value": "[variables('templateUrl')]"
          },
          "extensionUrl": {
            "value": "[variables('extensionUrl')]"
          },
          "uniqueString": {
            "value": "[variables('uniqueString')]"
          }
        }
      }
    }
  ],
  "outputs": {
    "nodeAdminURL": {
      "type": "string",
      "value": "[concat('http://vm0-', variables('uniqueString'), '.', resourceGroup().location, '.cloudapp.azure.com:8091')]"
    }
  }
}
