{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "metadata": {
    "_generator": {
      "name": "bicep",
      "version": "0.19.5.34762",
      "templateHash": "459189603834854648"
    }
  },
  "parameters": {
    "ipExtendedCommunityName": {
      "type": "string",
      "metadata": {
        "description": "Name of the Ip Extended Community"
      }
    },
    "location": {
      "type": "string",
      "defaultValue": "[resourceGroup().location]",
      "metadata": {
        "description": "Azure Region for deployment of the Ip Prefix and associated resources"
      }
    },
    "annotation": {
      "type": "string",
      "defaultValue": "",
      "metadata": {
        "description": "Switch configuration description"
      }
    },
    "ipExtendedCommunityRules": {
      "type": "array",
      "metadata": {
        "description": "List of IP Extended Community Rules"
      }
    }
  },
  "resources": [
    {
      "type": "Microsoft.ManagedNetworkFabric/ipExtendedCommunities",
      "apiVersion": "2023-06-15",
      "name": "[parameters('ipExtendedCommunityName')]",
      "location": "[parameters('location')]",
      "properties": {
        "copy": [
          {
            "name": "ipExtendedCommunityRules",
            "count": "[length(range(0, length(parameters('ipExtendedCommunityRules'))))]",
            "input": {
              "action": "[parameters('ipExtendedCommunityRules')[range(0, length(parameters('ipExtendedCommunityRules')))[copyIndex('ipExtendedCommunityRules')]].action]",
              "sequenceNumber": "[parameters('ipExtendedCommunityRules')[range(0, length(parameters('ipExtendedCommunityRules')))[copyIndex('ipExtendedCommunityRules')]].sequenceNumber]",
              "routeTargets": "[parameters('ipExtendedCommunityRules')[range(0, length(parameters('ipExtendedCommunityRules')))[copyIndex('ipExtendedCommunityRules')]].routeTargets]"
            }
          }
        ],
        "annotation": "[if(not(empty(parameters('annotation'))), parameters('annotation'), null())]"
      }
    }
  ],
  "outputs": {
    "resourceID": {
      "type": "string",
      "value": "[resourceId('Microsoft.ManagedNetworkFabric/ipExtendedCommunities', parameters('ipExtendedCommunityName'))]"
    }
  }
}