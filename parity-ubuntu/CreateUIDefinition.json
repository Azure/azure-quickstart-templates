{
  "handler": "Microsoft.Compute.MultiVm",
  "version": "0.0.1-preview",
  "parameters": {
    "basics": [
      {
        "name": "vmDnsPrefix",
        "type": "Microsoft.Common.TextBox",
        "label": "VM DNS Prefix",
        "toolTip": "This is the unique DNS name of the for the public IP for your VM",
        "constraints": {
          "required": true,
          "regex": "^[a-z0-9]{3,24}$",
          "validationMessage": "DNS name must be between 3 and 24 characters long, and can contain only numbers and lowercase letters."
        }
      },
      {
        "name": "adminUsername",
        "type": "Microsoft.Compute.UserNameTextBox",
        "label": "Username",
        "toolTip": "Admin username for the virtual machine.",
        "osPlatform": "Linux"
      },
      {
        "name": "adminPassword",
        "type": "Microsoft.Compute.PasswordBox",
        "label": "Password",
        "toolTip": "Admin password for the virtual machine.",
        "osPlatform": "Linux"
      }
    ],
    "steps": [],
    "outputs": {
      
    }
  }
}
